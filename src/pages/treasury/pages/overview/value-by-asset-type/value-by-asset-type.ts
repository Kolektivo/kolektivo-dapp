import './value-by-asset-type.scss';
import { ICustomElementViewModel } from 'aurelia';
export class ValueByAssetType implements ICustomElementViewModel {
  constructor() {
    // you can inject the element or any DI in the constructor
  }
}
